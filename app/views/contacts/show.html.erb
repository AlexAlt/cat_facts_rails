<div class="contact-card">
  <%= render 'contact_show_tile', contact: @contact %>
</div>

<p><%= link_to "Update Catact", edit_contact_path(@contact), id: 'update-contact', remote: true  %></p>
<p><%= link_to "Back to All Catacts", contacts_path %></p>
