<h1>Contacts</h1>
<p>
  <%= link_to "Add new Catact", new_contact_path %>
</p>
<ul class="contacts-list">
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, contact %>
      <ul>
        <li><%= contact.phonenumber %></li>
      </ul>
    </li>
  <% end %>
</ul>
